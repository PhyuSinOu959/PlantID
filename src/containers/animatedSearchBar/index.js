import React from "react";
import { View } from "react-native";
import UI from "./UI"

export default function AnimatedSearchBar({ navigation }) {
    return (
        <View>
            <UI />
        </View>
    );
}
