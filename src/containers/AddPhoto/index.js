import React from "react";
import { View } from "react-native";
import AddPhoto_UI from "./AddPhoto_UI"

const AddPhoto = () => {
    return (
        <View>
            <AddPhoto_UI />
        </View>
    );
}
export default AddPhoto;